const fs = require('fs');

let stations;

// fs.readFile('./message.txt', (err, data) => {
//   if (err) throw err;
//   stations = data;
//   console.log(stations.toString());
// });

console.log(stations.toString());

module.exports = stations;
